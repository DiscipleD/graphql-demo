{
  "name": "application-name",
  "version": "0.0.1",
  "dependencies": {
    "babel-cli": "^6.11.4",
    "babel-preset-es2015": "^6.9.0",
    "express": "^4.14.0",
    "express-graphql": "^0.5.3",
    "graphql": "^0.6.2",
    "json-loader": "^0.5.4",
    "nodemon": "^1.10.0",
    "webpack": "^1.13.1"
  },
  "scripts": {
    "start": "npm run build & npm run server",
    "build": "babel src -w -d build",
    "server": "nodemon --watch build --delay 2 build/server.js"
  }
}
